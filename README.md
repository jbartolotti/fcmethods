# fcmethods

A Python library for functional connectivity analyses on BIDS datasets with BIDS-compliant output.

## Installation

```bash
pip install -e .
```

## Features

- Parse timecourse data from CSV format
- Export timecourses to BIDS-compliant derivative structure
- Basic functional connectivity analyses

## Usage

See examples in the `examples/` directory.
